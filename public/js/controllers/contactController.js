'use strict';
var app = app || {};
(function(module){

  const contactController = {};

  contactController.init = function() {
    // $('section').hide();
    // $('.template').hide();
    // $('.texture').fadeIn();
    // $('.navTab').hide();

  }

  module.contactController = contactController;
})(app);
